import React from 'react';
const MOODS = ['Luxury','Energetic','Minimal','Playful','Cinematic','Emotional','Corporate','Bold','Calm','Tech'];
const HOOKS = ['Pain-Point','Curiosity Gap','Bold Claim','Social Proof','Direct Offer','Question','Shocking Stat'];
const OBJECTIVES = ['awareness','consideration','conversion','retention','app_install'];
export default function DirectorControls({ mood, hookType, objective, onMoodChange, onHookChange, onObjectiveChange }: { mood: string; hookType: string; objective: string; onMoodChange: (m: string) => void; onHookChange: (h: string) => void; onObjectiveChange: (o: string) => void }) {
  return (<div className="bg-white/5 rounded-xl p-4 space-y-4"><h3 className="text-sm font-semibold text-white/70 uppercase tracking-wider">Creative Direction</h3><div><label className="text-xs text-white/50 mb-1 block">Mood</label><div className="flex flex-wrap gap-1.5">{MOODS.map(m => (<button key={m} onClick={() => onMoodChange(m)} className={`px-3 py-1 rounded-full text-xs font-medium transition-all ${mood === m ? 'bg-indigo-500 text-white' : 'bg-white/10 text-white/60 hover:bg-white/20'}`}>{m}</button>))}</div></div><div><label className="text-xs text-white/50 mb-1 block">Hook Strategy</label><select value={hookType} onChange={e => onHookChange(e.target.value)} className="w-full bg-white/10 text-white text-sm rounded-lg px-3 py-2 border border-white/10">{HOOKS.map(h => <option key={h} value={h}>{h}</option>)}</select></div><div><label className="text-xs text-white/50 mb-1 block">Ad Objective</label><select value={objective} onChange={e => onObjectiveChange(e.target.value)} className="w-full bg-white/10 text-white text-sm rounded-lg px-3 py-2 border border-white/10">{OBJECTIVES.map(o => <option key={o} value={o}>{o.charAt(0).toUpperCase() + o.slice(1)}</option>)}</select></div></div>);
}
