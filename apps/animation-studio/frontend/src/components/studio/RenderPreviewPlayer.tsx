import React, { useState } from 'react';
export default function RenderPreviewPlayer({ videoUrl, thumbnailUrl, status, progress }: { videoUrl?: string; thumbnailUrl?: string; status: string; progress: number; renderJobId: string }) {
  return (<div className="space-y-2"><h3 className="text-sm font-semibold text-white/70 uppercase tracking-wider">Preview</h3><div className="relative aspect-video bg-black/40 rounded-xl overflow-hidden border border-white/10">{videoUrl && status === 'complete' ? (<video src={videoUrl} poster={thumbnailUrl} controls className="w-full h-full object-contain" />) : (<div className="absolute inset-0 flex flex-col items-center justify-center">{['processing','scene_rendering','mixing'].includes(status) ? (<><div className="w-12 h-12 border-2 border-indigo-400 border-t-transparent rounded-full animate-spin mb-3" /><p className="text-sm text-white/60">{status.replace('_',' ')}...</p><div className="w-48 h-1.5 bg-white/10 rounded-full mt-2 overflow-hidden"><div className="h-full bg-indigo-500 rounded-full transition-all duration-500" style={{ width: `${progress}%` }} /></div><p className="text-xs text-white/30 mt-1">{progress}%</p></>) : thumbnailUrl ? (<img src={thumbnailUrl} alt="Preview" className="w-full h-full object-contain opacity-60" />) : (<p className="text-white/30 text-sm">No preview available</p>)}</div>)}</div></div>);
}
